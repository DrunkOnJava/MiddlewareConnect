import SwiftUI

extension CardView {
    /// Card style options for the `CardView` component
    enum Style {
        case standard
        case elevated
        case bordered
        case interactive  // For cards that can be tapped
    }
}